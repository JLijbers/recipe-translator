We have a web application that takes a URL and translates the recipe from that page to Dutch.

We want the application to automatically detect the language the recipe is written in (and display the detected language).
